# How to merge the branches to main branch?
-> First know the your postion which branch you have
-> After check your position 
-> Get the command:-
                    git merge branch_name

# Merge a conflic file?
-> If devloper work both branch as same time 
-> Then you check the both code and find a mistke in one branch
-> now use command:-
                    git merge old_branch_name

        
# Now all about the stash 
-> Stash is a temparary storage like stack.

# When use a stash?
-> Stach will use when you write somthing but you don't want to commit right
-> So stash will move data into temporrary storage so 
-> Without interfier user work both branch

# How to use stach with cmds?
-> git stash          :- move into stash 
-> git stash list     :- Show the existing list
-> git stash apply    :- All stash back into orignal place
-> git stash apply@{0}:- Apply stash for some specific stash
<<<<<<< Updated upstream
<<<<<<< Updated upstream
-> git stash pop      :- Delete all the stash and apply all the stash

# What is a git tags?
 -> Git tags are a one type of sticky notes,
 -> Way to make a specific points in your repositery,
 
# When we use git tags?
 -> An git tags will use when
 -> You know a specific version of your programs and 
 -> You want some extra and specific commit.
 -> Widly use when you want to uploade some major version.

# Git tags cmds..
 -> git tag <tag_name>                          -: reate a git tags 
 -> git tag -a <tag_name> -m "Commit meessage"   :-Create a tag with anotation commit message
 -> git tag <show all working tags>              :- Show all the text which you crate 
-> git stash pop      :- Delete all the stash and apply all the stash
